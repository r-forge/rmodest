\name{exlm}
\alias{exlm}
\docType{data}
\title{ ~~ data name/kind ... ~~}
\description{
  ~~ A concise (1-5 lines) description of the dataset. ~~
}
\usage{data(exlm)}
\format{
  The format is:
  expression(log((c + a * exp(b * x)/(1 + s * a * (exp(b * x) -      1)/b)) * (exp(-c * x) * (1 + s * a * (exp(b * x) - 1)/b)^(-1/s))))
}
\details{
  ~~ If necessary, more details than the __description__ above ~~
}
\source{
  ~~ reference to a publication or URL from which the data were obtained ~~
}
\references{
  ~~ possibly secondary sources and usages ~~
}
\examples{
data(exlm)
## maybe str(exlm) ; plot(exlm) ...
}
\keyword{datasets}
